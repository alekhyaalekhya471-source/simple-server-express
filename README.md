# simple-server-express
A simple REST API built with Node.js and Express.js that demonstrates basic routing and JSON request handling. Features GET / – Returns a JSON welcome message. POST /echo – Accepts a JSON request body and returns the same data in the response. Uses Express middleware to parse JSON requests.
